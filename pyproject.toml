[project]
name = "polars-dbn"
version = "0.1.0"
description = "Polars IO plugin for reading the Databento Binary Encoding (DBN) format."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "databento>=0.66.0",
    "numpy>=2.2.6",
    "polars>=1.35.2",
    "pyarrow>=22.0.0",
]
